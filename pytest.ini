[pytest]
# Tell pytest-asyncio to automatically handle async test functions
# Without this, async tests would be skipped silently
asyncio_mode = auto

# Show test names verbosely by default
addopts = -v

# Where to find tests
testpaths = tests